export default function Home() {
  // No public-facing landing page is specified for this service (UX doc scopes it
  // to /admin/* and /api/*) — this stub exists only so the App Router has a root route.
  return null;
}
