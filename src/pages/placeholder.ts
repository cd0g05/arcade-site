/**
 * Shared entry for placeholder pages (hub shell, style-guide stub, cabinet
 * stubs). Loads the style system only. Real page modules replace this in
 * later partitions (feat/hub-games, feat/cabinets-*).
 */
import "../styles/main.css";
